services:
  # Web Service (Backend + Frontend)
  - type: web
    name: taskify-smart-pantry
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
    autoDeploy: true
